name: CD - Deploy to Azure

on:
  push:
    branches: ["feature/backend/unitTesting_p1"]
    paths:
      - "src/backend/**"
      - "src/web/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: cd-deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.set_tag.outputs.image_tag }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Login to ACR
        env:
          ACR: ${{ secrets.ACR_REGISTRY }}
        run: az acr login --name "${ACR%%.*}"

      - name: Set image tag
        id: set_tag
        run: echo "image_tag=${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Build and push Backend image
        run: |
          docker build -t ${{ secrets.ACR_REGISTRY }}/unitrade-backend:${{github.sha}} -f src/backend/Api/Dockerfile .
          docker push ${{ secrets.ACR_REGISTRY }}/unitrade-backend:${{github.sha}}

      - name: Build and push Frontend image
        run: |
          docker build -t ${{ secrets.ACR_REGISTRY }}/unitrade-frontend:${{github.sha}} src/web
          docker push ${{ secrets.ACR_REGISTRY }}/unitrade-frontend:${{github.sha}}

  deploy-staging:
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: staging
    env:
      IMAGE_TAG: ${{ needs.build-and-push.outputs.image_tag }}
      #RG: ${{ secrets.AZURE_RESOURCE_GROUP }}
      #PUT rgs into secrets
      BACKEND_RG: unitrade-BackEnd
      FRONTEND_RG: unitrade-FrontEnd
      BACKEND_APP: unitrade-backend-staging
      FRONTEND_APP: unitrade-frontend-staging
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Instal EF tool
        run: dotnet tool install --global dotnet-ef
      
      - name: restore dependencies
        run: dotnet restore UniTrade.sln

      - name: Run migrations (staging)
        working-directory: src/backend/Api
        env:
          ConnectionStrings__DefaultConnection: ${{secrets.DB_CONNECTION_STRING_STAGING}}
          #do it the longer way after deploying!
          Acs__ConnectionString: ${{ secrets.Acs__ConnectionString }}
        run: dotnet ef database update --project ../Infrastructure --startup-project .

      - name: Deploy backend green revision
        id: backend_rev
        run: |
          SUFFIX="g${IMAGE_TAG:0:7}-${{github.run_attempt}}"
          az containerapp update \
              --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
              --image "${{ secrets.ACR_REGISTRY }}/unitrade-backend:${IMAGE_TAG}" \
              --revision-suffix "$SUFFIX" \
              --set-env-vars \
                "ConnectionStrings__DefaultConnection=${{ secrets.DB_CONNECTION_STRING_STAGING }}" \
                "Acs__ConnectionString=${{ secrets.Acs__ConnectionString }}" \
                "Jwt__Secret=${{ secrets.JWT_Secret}}" \
                "Cors__AllowedOrigins=${{secrets.FRONTEND_URL_STAGING}}" 
          echo "suffix=$SUFFIX" >> "$GITHUB_OUTPUT"

      - name: Deploy frontend green revision
        id: front_rev
        run: |
          SUFFIX="g${IMAGE_TAG:0:7}-${{github.run_attempt}}"
          az containerapp update \
              --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" \
              --image "${{ secrets.ACR_REGISTRY }}/unitrade-frontend:${IMAGE_TAG}" \
              --revision-suffix "$SUFFIX" \
              --set-env-vars \
              "VITE_API_URL=${{ secrets.VITE_API_URL_STAGING }}"
          echo "suffix=$SUFFIX" >> "$GITHUB_OUTPUT"
      - name: Wait for backend revision to be Running
        run: |
          az containerapp revision show \
          --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
          --revision "${BACKEND_APP}--${{ steps.backend_rev.outputs.suffix }}" \
          --query "properties.healthState" -o tsv
      - name: Health check staging backend (green)
        run: |
          FQDN=$(az containerapp revision show \
          --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
          --revision "${BACKEND_APP}--${{ steps.backend_rev.outputs.suffix }}" \
           --query "properties.fqdn" -o tsv)
          for i in $(seq 1 10); do
             if curl -fsS "https://$FQDN/health"; then exit 0; fi
             sleep 7
          done
          echo "Health check failed" && exit 1
      - name: Shift traffic to green (backend)
        run: |
          az containerapp ingress traffic set \
          --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
          --revision-weight "${BACKEND_APP}"--${{ steps.backend_rev.outputs.suffix }}=100

      - name: Shift traffic to green (frontend)
        run: |
          az containerapp ingress traffic set \
          --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" \
          --revision-weight "${FRONTEND_APP}"--${{ steps.front_rev.outputs.suffix }}=100

      - name: Smoke test staging
        run: |
          curl -fsS "${{ secrets.VITE_API_URL_STAGING }}/health" > /dev/null

      - name: Deactivate old backend revisions
        run: |
          az containerapp revision list --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
          --query "[?properties.trafficWeight=='0']name" -o tsv | \
          while read -r rev; do
            az containerapp revision deactivate --name "$BACKEND_APP" --resource-group "$BACKEND_RG" --revision "$rev"
          done
      - name: Deactivate old frontend revisions
        run: |
          az containerapp revision list --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" \
          --query "[?properties.trafficWeight=='0']name" -o tsv | \
          while read -r rev; do
            az containerapp revision deactivate --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" --revision "$rev"
          done

  approval-gate:
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment: production-approval
    steps:
      - run: echo "Approved for production deploy"

  deploy-prod:
    needs: [build-and-push, approval-gate]
    runs-on: ubuntu-latest
    environment: production
    env:
      IMAGE_TAG: ${{ needs.build-and-push.outputs.image_tag }}
      BACKEND_RG: unitrade-BackEnd
      FRONTEND_RG: unitrade-FrontEnd
      BACKEND_APP: unitrade-backend
      FRONTEND_APP: unitrade-frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Instal EF tool
        run: dotnet tool install --global dotnet-ef

      - name: restore dependencies
        run: dotnet restore UniTrade.sln

      - name: Run migrations (prod)
        working-directory: src/backend/Api
        env:
          ConnectionStrings__DefaultConnection: ${{ secrets.DB_CONNECTION_STRING_PROD }}
          Acs__ConnectionString: ${{ secrets.Acs__ConnectionString }}
        run: dotnet ef database update --project ../Infrastructure --startup-project .

      - name: Deploy backend green revision
        id: backend_rev
        run: |
          SUFFIX="g${IMAGE_TAG:0:7}-${{github.run_attempt}}"
          az containerapp update \
              --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
              --image "${{ secrets.ACR_REGISTRY }}/unitrade-backend:${IMAGE_TAG}" \
              --revision-suffix "$SUFFIX" \
              --set-env-vars \
                "ConnectionStrings__DefaultConnection=${{ secrets.DB_CONNECTION_STRING_PROD }}" \
                "Acs__ConnectionString=${{ secrets.Acs__ConnectionString }}" \
                "Jwt__Secret=${{ secrets.JWT_Secret}}" \
                "Cors__AllowedOrigins=${{secrets.FRONTEND_URL}}" 
          echo "suffix=$SUFFIX" >> "$GITHUB_OUTPUT"

      - name: Deploy frontend green revision
        id: front_rev
        run: |
          SUFFIX="g${IMAGE_TAG:0:7}-${{github.run_attempt}}"
          az containerapp update \
              --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" \
              --image "${{ secrets.ACR_REGISTRY }}/unitrade-frontend:${IMAGE_TAG}" \
              --revision-suffix "$SUFFIX" \
              --set-env-vars \
              "VITE_API_URL=${{ secrets.VITE_API_URL }}"
          echo "suffix=$SUFFIX" >> "$GITHUB_OUTPUT"

      - name: Health check staging backend (green)
        run: |
          FQDN=$(az containerapp revision show \
          --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
          --revision "${BACKEND_APP}--${{ steps.backend_rev.outputs.suffix }}" \
            --query "properties.fqdn" -o tsv)
          for i in $(seq 1 10); do
              if curl -fsS "https://$FQDN/health"; then exit 0; fi
              sleep 7
          done
          echo "Health check failed" && exit 1
      - name: Shift traffic to green (backend)
        run: |
          az containerapp ingress traffic set \
          --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
          --revision-weight "${BACKEND_APP}"--${{ steps.backend_rev.outputs.suffix }}=100

      - name: Shift traffic to green (frontend)
        run: |
          az containerapp ingress traffic set \
          --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" \
          --revision-weight "${FRONTEND_APP}"--${{ steps.front_rev.outputs.suffix }}=100

      - name: Smoke test prod
        run: |
          curl -fsS "${{ secrets.VITE_API_URL }}/health" > /dev/null

      - name: Deactivate old backend revisions
        run: |
          az containerapp revision list --name "$BACKEND_APP" --resource-group "$BACKEND_RG" \
          --query "[?properties.trafficWeight=='0']name" -o tsv | \
          while read -r rev; do
            az containerapp revision deactivate --name "$BACKEND_APP" --resource-group "$BACKEND_RG" --revision "$rev"
          done
      - name: Deactivate old frontend revisions
        run: |
          az containerapp revision list --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" \
            --query "[?properties.trafficWeight=='0']name" -o tsv | \
            while read -r rev; do
              az containerapp revision deactivate --name "$FRONTEND_APP" --resource-group "$FRONTEND_RG" --revision "$rev"
            done